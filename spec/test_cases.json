{
  "version": "1.0.0",
  "target_metric": "f1_score",
  "target_threshold": 0.95,
  "cases": [
    {
      "id": "case_001",
      "name": "基本功能验证",
      "category": "core",
      "weight": 1.0,
      "input": { "type": "json", "data": { "query": "hello world" } },
      "expected": { "type": "exact_match", "value": "HELLO WORLD" },
      "timeout_ms": 1000
    },
    {
      "id": "case_002",
      "name": "空输入边界",
      "category": "edge",
      "weight": 0.8,
      "input": { "type": "json", "data": { "query": "" } },
      "expected": {
        "type": "schema",
        "schema": { "error": { "type": "string", "required": true } }
      },
      "timeout_ms": 500
    }
  ],
  "tester_instructions": "主动构造额外边界情况，目标是让系统失败。"
}
