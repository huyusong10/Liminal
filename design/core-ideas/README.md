# 核心思想

## 0. 最高产品原则

Loopora 是面向长期 AI Agent 任务的本地任务平台。更根本地说，它把未来长期任务里原本需要人类反复做的判断、纠偏、取证和阻断，提前编译成一个可运行的 human-shaped Loop。

核心主工作流是：

`编排 Loop -> 运行 Loop -> 自动迭代并收集证据 -> 输出运行状态、Loop 裁决与结果`

`product-principle.md` 是本目录的最高原则。下方公理都服务于同一件事：让长期任务不只依赖 Agent 自律，而是继承用户为当前任务外化出来的判断结构，并被外部编排、运行、观察和裁决。

如果讨论卡在 `Loop / bundle / run / alignment session` 等术语层级上，先读 `concept-map.md`。它只负责澄清概念分层、主工作流和场景边界，不新增运行契约。

## 1. 设计公理

1. **Loop 必须被人类判断塑形。**
   Loopora 的核心不是延长 Agent 时间，而是把 task-scoped human judgment 编译成 `spec / roles / workflow / evidence`。

2. **长期任务必须外部编排。**
   关键判断不能只藏在当前 Agent 对话里，必须能被保存、预览、运行，并留下可复盘证据。

3. **loop 的理由是新证据。**
   如果下一轮不会产生新的外部反馈，loop 就会从收敛器退化成随机游走。

4. **生成器不能裁决自己。**
   Builder 可以改动；Inspector 负责取证；GateKeeper 负责是否结束。

5. **任务契约必须冻结。**
   `Task`、checks、guardrails 和完成边界在一次 run 内不能被角色静默改写。

6. **判断必须落到运行面。**
   用户姿态必须投影到 `spec / roles / workflow`，不能只留在 prompt 文案。

7. **证据必须可追溯。**
   每次 run 必须留下结构化产物、事件流和摘要；没有产物就等于没有发生。

8. **证据应能被用户带走。**
   run 后用户如何调整 Loop 属于用户掌控的高阶行为；Loopora 只负责让证据、方案文件、导入 / 导出和可选对话改进入口清楚可用，不维护 Loop 演化历史。

9. **workdir 是用户资产。**
   Loopora 可以修改目标目录，但不得把破坏性重建当作默认策略。

10. **接口层必须同构。**
   Web、CLI、API 表达同一套 loop 和 bundle 语义，不允许核心能力长期只存在于单一入口。

11. **系统首先是本地治理器。**
   状态、锁、日志、证据和产物优先围绕本地目录和本地数据库组织。

## 2. 不变量

- 单个 workdir 同一时刻最多允许一个活动 run。
- run 的主要事实源必须可从 `.loopora/` 与 `app.db` 还原。
- `Inspector` / `GateKeeper` / `Guide` 默认不应修改源文件。
- `GateKeeper` 的通过结论必须可映射到 checks、metrics、证据或边界约束。
- `Role Notes` / `posture_notes` 只能改变角色工作姿态，不能静默改变全局成功标准。
- `run status` 只能说明系统生命周期；Loop 是否达标必须由 evidence 和 task verdict 表达。
- `.loopora/` 是系统保留空间；业务改动不应写入该目录。

## 3. 反例

| 反例 | 违反规则 | 后果 |
| --- | --- | --- |
| 让 Agent 在没有新证据的前提下继续空转几轮 | loop 的理由是新证据 | 叙事漂移，任务变成盲盒 |
| 每轮都重新生成 checks | 任务契约必须冻结 | 分数不可比较，停滞检测失真 |
| Builder 自己宣布完成 | 生成器不能裁决自己 | 通过条件退化成自我报告 |
| Inspector 为了通过检查而改代码 | 角色职责混杂 | 证据与修复混在一起 |
| 只在 prompt 里描述用户偏好 | 判断没有运行面 | 长任务中容易遗忘或漂移 |
| 只延长循环时间，不外化判断结构 | Loop 没有人类判断塑形 | 错误会被后续轮次放大成自洽叙事 |
| 只记录日志，不留 evidence artifact | 证据不可追溯 | 无法复盘、比较或外部再利用 |
| Web 能做核心能力，CLI/API 无语义入口 | 接口层不同构 | 系统出现双重能力模型 |
| 把 run succeeded 当作 Loop 通过 | 运行状态与 Loop 裁决混淆 | 用户误信未证明的结果 |

## 4. 非目标

- 不是通用 DAG 编排引擎。
- 不是多租户远程 SaaS。
- 不是长期人格记忆系统。
- 不是 benchmark 刷分器。
- 不是替代底层 AI Agent CLI 的二次聊天界面。
- 不是把用户判断静默写入模型权重的训练系统。

## 5. 压缩判断式

- 如果某条信息不能帮助“外化人类判断 / 编排 Loop / 运行 Loop / 自动迭代 / 复盘证据”，它就不属于核心设计。
- 如果某个能力只能在一个入口存在，它就不是完成态。
- 如果一个 run 结束后不能回答“系统是否正常结束、任务是否被证明、改了什么、怎么测的、为什么过/没过”，该设计就是不合格的。
