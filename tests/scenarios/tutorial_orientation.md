本用例由 Agent 自行进行模拟

# 场景：用户通过教程重新判断是否需要 Loopora

**目标**：确认教程页帮助用户判断何时需要外部化 Loop 治理，而不是把 Loopora 讲成 YAML 生成器、提示词包或手工流程平台。

**前置**：用户可能是首次进入，也可能已经用过一次后回来重新选择路径。

**步骤**：

1. 打开教程页，确认页面先解释为什么需要外部治理，再介绍 Loop 的创建路径。
2. 确认教程把姿态落到 `spec / roles / workflow`，而不是说成一段提示词。
3. 查看决策树和流程示例，确认它们用于解释风险、证据和判断顺序，不要求新用户手写流程。
4. 从教程进入默认对话编排路径、手动编排路径和技能工具入口，确认三者边界清楚。
5. 对已有方案包或运行的用户，确认页面能展示证据结论和导入 / 导出边界，而不是把方案包迭代包装成系统职责。

**预期**：

- 新用户能先判断是否真的需要 Loopora，再进入对话编排 Loop。
- 手动编排和已有 YAML 导入仍可达，但不盖过默认主路径。
- 教程描述的是稳定治理语义，不依赖某一种展示语言或具体静态文案。
