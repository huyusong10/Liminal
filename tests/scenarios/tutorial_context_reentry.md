本用例由 Agent 自行进行模拟

# 场景：用户回到教程页重新判断职责层级与上下文流转

**目标**：确认教程页在长内容阅读场景下仍然清楚分隔“职责分流”和“context 流转”两部分，用户能快速找到该回哪一层修改。

**前置**：用户已经用过一次 Loopora，但隔了一段时间后回来，想重新确认该改角色、编排还是 loop。

**步骤**：
1. 打开教程页，先阅读顶部标题和第一组“先判断这次改动属于哪一层”的卡片。
2. 依次点击“角色定义”“流程编排”“创建循环”三张卡片，确认页面能直接跳到对应章节。
3. 在每个章节里快速扫读 checklist，确认“改 prompt / 工具 / 默认模型”“改顺序 / 开关步骤 / 收束逻辑”“换项目 / spec / 轮次策略”仍然清楚分层。
4. 继续滚到 context flow 区域，确认 run contract、step context、handoff 和 iteration summary 这四个阶段仍保持稳定顺序，且能在同一页连续阅读。

**预期**：教程页即使在长篇阅读场景下也不会丢失层级感；用户能先定位职责层，再继续理解 context 如何在流程中流转。
