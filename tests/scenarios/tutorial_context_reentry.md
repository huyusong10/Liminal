本用例由 Agent 自行进行模拟

# 场景：用户回到教程页重新判断 bundle 与手动资产的关系

**目标**：确认教程页在长内容阅读场景下仍然清楚说明默认 bundle-first 路径，以及手动角色 / 编排 / loop 编辑为什么只是 expert mode。

**前置**：用户已经用过一次 Loopora，但隔了一段时间后回来，想重新确认“是重新生成 bundle，还是手动改某个资产”。

**步骤**：
1. 打开教程页，先阅读顶部标题和核心精神卡片。
2. 确认页面明确说明 working agreement 是对齐阶段的临时确认物，最终导入的是 YAML bundle。
3. 确认页面说明 bundle 会同时物化 spec、角色定义、workflow 和 loop，不需要用户先手工拆成多个资产。
4. 点击“安装对齐 Skill”“导入 Bundle 创建循环”“手动专家模式”等入口，确认它们指向对应页面或锚点。
5. 浏览下方 5 个 workflow 样例，确认它们仍然用于解释姿态如何影响流程形状，而不是要求新用户先手写流程。

**预期**：教程页即使在长篇阅读场景下也不会丢失主线；用户能优先选择 bundle revision 路径，并在确实知道要改哪个 surface 时再进入手动专家模式。
